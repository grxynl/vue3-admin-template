/** cache默认缓存时间 3天 */
export const DEFAULT_CACHE_TIME =  60 * 60 * 24 * 3

/** 动态路由开关 */
export const OPEN_ASYNC_ROUTER = true

/** 约定重新登录code */
export const LOG_OUT_CODE = 10001

/** 默认主题序号 */
export const NORMAL_THEME_INDEX = 0

/** keepAlive组件
 * 默认组件名为文件名，需要手动修改name值，不然默认index
 * */
export const KEEP_ALIVE_COMPONENT = ['dashboard']

